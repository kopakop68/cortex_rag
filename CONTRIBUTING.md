# Contributing

- Create a feature branch from `develop`: `git checkout -b feat/<short-name>`
- Run linters/tests: `ruff check . && pytest`
- Use Conventional Commits: `feat:`, `fix:`, `docs:`, `chore:`
- Open a PR into `main` with a clear description and screenshots for UI changes.
